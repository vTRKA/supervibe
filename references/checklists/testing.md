# Testing Reference Pack

- Identify the smallest command that proves the changed behavior.
- Run narrow tests during implementation and full project tests only at the final graph/release gate.
- Record command, exit code, and failing diagnostics.
- Do not claim regression coverage without a test or an explicit residual-risk note.
