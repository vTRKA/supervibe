# CLAUDE.md

> Claude Code host instruction file generated by `supervibe:genesis` for stack: **Laravel 11 + Next.js 14 + Postgres + Redis** with bounded contexts: {{bounded-contexts}}. Other hosts receive equivalent Supervibe managed blocks through their own adapter files.

This repository uses the strict Supervibe workflow workflow.

## Mission

Deliver correct, minimal, verifiable changes for a **Laravel + Next.js + Postgres + Redis** application.

## Non-negotiable rules

1. Explore first, then plan, then implement, then verify.
2. Do not invent files, commands, routes, events, DTOs, stores, SQL objects, or public contracts.
3. Reuse existing project patterns before creating new abstractions.
4. Keep the **backend ↔ frontend** layer boundary explicit.
5. Do not move business logic into the UI because it is convenient.
6. Prefer incremental changes with reviewable stop points.
7. Do not say a task is done without evidence (`supervibe:verification` skill).
8. **Never claim "works correctly" without running a verification command and showing its output.**
9. Every artifact MUST score ≥9 against its `*-quality` rubric (see `supervibe:confidence-scoring`).
10. Never use `git stash`/`stash pop`, `push --force`, `reset --hard`, or any banned command (see the selected host settings deny-list when supported).
11. Conventional Commits enforced via commitlint (`commitlint.config.js`).
12. Pre-commit pipeline enforced via husky (`.husky/`).
13. Zero dead code: every function/struct/variant MUST have ≥1 live call site (see `rules/no-dead-code.md`).

## Project paths (authoritative)

- **Backend (Laravel)**: `backend/`
  - Modules: `backend/app/Modules/{{bounded-contexts}}`
  - Routes: `backend/routes/api.php`, `backend/routes/web.php`
  - Migrations: `backend/database/migrations/`
  - Tests: `backend/tests/Feature/`, `backend/tests/Unit/` (Pest)
- **Frontend (Next.js)**: `frontend/`
  - App router: `frontend/app/`
  - Components: `frontend/components/` (FSD layers)
  - Tests: `frontend/__tests__/`
- **Prototypes**: `.supervibe/artifacts/prototypes/`
  - **Brandbook direction**: `.supervibe/artifacts/brandbook/`
  - **Design system source of truth**: `.supervibe/artifacts/prototypes/_design-system/`
- **Docs**: `docs/`
  - ADRs: `.supervibe/artifacts/adr/`
  - PRDs: `.supervibe/artifacts/prd/`
  - Specs: `.supervibe/artifacts/specs/`
  - Plans: `.supervibe/artifacts/plans/`

## Agent routing — who to dispatch for what

| Task type | Agent (`subagent_type`) | Skills |
|-----------|------------------------|--------|
| Explore code / find files | `Explore` (built-in) | — |
| Map repo before work | `supervibe:_core:repo-researcher` | repo discovery, anti-hallucination |
| Define requirements | `supervibe:_product:systems-analyst` | requirements-intake |
| Product / strategy / OKR | `supervibe:_product:product-manager` (CPO scope) | prd, adr |
| Backend: modules, services, models | `supervibe:stacks/laravel:laravel-developer` | tdd, verification, code-review |
| Backend: architecture | `supervibe:stacks/laravel:laravel-architect` (READ-ONLY) | adr |
| Backend: queue jobs | `supervibe:stacks/laravel:queue-worker-architect` | adr, debugging |
| Backend: Eloquent models | `supervibe:stacks/laravel:eloquent-modeler` | verification |
| Frontend: pages, components | `supervibe:stacks/nextjs:nextjs-developer` | tdd, verification, code-review |
| Frontend: architecture | `supervibe:stacks/nextjs:nextjs-architect` (READ-ONLY) | adr |
| Frontend: server actions | `supervibe:stacks/nextjs:server-actions-specialist` | tdd |
| Database: schema, migrations | `supervibe:stacks/postgres:postgres-architect` + `supervibe:_ops:db-reviewer` | adr, verification |
| Cache / Redis | `supervibe:stacks/redis:redis-architect` | adr |
| AI / LLM integration | `supervibe:_ops:ai-integration-architect` | adr |
| Infra: Sentinel, replicas, sharding | `supervibe:_ops:infrastructure-architect` | adr |
| Debug / investigate failure | `supervibe:_core:root-cause-debugger` | systematic-debugging |
| Code review | `supervibe:_core:code-reviewer` | code-review |
| Security audit | `supervibe:_core:security-auditor` (READ-ONLY) | code-review |
| Architecture review | `supervibe:_core:architect-reviewer` (READ-ONLY) | adr |
| Final quality gate | `supervibe:_core:quality-gate-reviewer` | confidence-scoring |
| Refactoring | `supervibe:_core:refactoring-specialist` | tdd, code-review |
| Design system / brand direction | `supervibe:_design:creative-director` | brandbook |
| Design new screen / UX | `supervibe:_design:ux-ui-designer` | prototype |
| HTML prototype builder | `supervibe:_design:prototype-builder` | prototype, brandbook |
| UI review / polish | `supervibe:_design:ui-polish-reviewer` | confidence-scoring |
| Accessibility review | `supervibe:_design:accessibility-reviewer` | confidence-scoring |
| Copy review | `supervibe:_design:copywriter` | confidence-scoring |
| QA / test strategy | `supervibe:_product:qa-test-engineer` | tdd, verification |
| Analytics tracking | `supervibe:_product:analytics-implementation` | confidence-scoring |
| SEO | `supervibe:_product:seo-specialist` | seo-audit |
| Email lifecycle | `supervibe:_product:email-lifecycle` | confidence-scoring |
| DevOps / SRE / CI/CD | `supervibe:_ops:devops-sre` | incident-response, adr |
| Performance | `supervibe:_ops:performance-reviewer` (READ-ONLY) | systematic-debugging |
| Dependencies | `supervibe:_ops:dependency-reviewer` (READ-ONLY) | confidence-scoring |
| API contracts | `supervibe:_ops:api-contract-reviewer` (READ-ONLY) | confidence-scoring |
| Rules curation | `supervibe:_meta:rules-curator` | rule-audit, sync-rules |
| Orchestration / what-to-run | `supervibe:_meta:supervibe-orchestrator` | confidence-scoring |
| Research current best practices | `supervibe:_ops:best-practices-researcher` | confidence-scoring |
| Research deps / CVEs / infra patterns | `supervibe:_ops:dependency-researcher` / `security-researcher` / `infra-pattern-researcher` | confidence-scoring |
| Competitive design research | `supervibe:_ops:competitive-design-researcher` | confidence-scoring |

### Scope boundaries (HARD rules)

- `laravel-developer` touches ONLY `backend/` — never `frontend/`
- `nextjs-developer` touches ONLY `frontend/` — never `backend/`
- `prototype-builder` touches ONLY `.supervibe/artifacts/prototypes/` — never `backend/` or `frontend/`
- `postgres-architect` touches ONLY `backend/database/migrations/` and DB-related service files
- READ-ONLY agents (no Write/Edit): `repo-researcher`, `code-reviewer`, `security-auditor`, `architect-reviewer`, `performance-reviewer`, `dependency-reviewer`, `db-reviewer`, `api-contract-reviewer`, `ui-polish-reviewer`, `accessibility-reviewer`, `laravel-architect`, `nextjs-architect`, all researchers, all designers except prototype-builder

### Common workflows

**New feature (full-stack):**
1. `supervibe:_product:systems-analyst` → requirements contract via `supervibe:requirements-intake`
2. `supervibe:_core:repo-researcher` → map existing code
3. `supervibe:_product:product-manager` → PRD if user-facing (via `supervibe:prd`)
4. `supervibe:brainstorming` if complexity ≥7
5. `supervibe:_design:creative-director` + `supervibe:_design:prototype-builder` if UI feature → `supervibe:prototype`
6. `supervibe:writing-plans` → phased plan
7. `supervibe:stacks/laravel:laravel-developer` → backend
8. `supervibe:stacks/nextjs:nextjs-developer` → frontend
9. `supervibe:_design:ui-polish-reviewer` → drift check
10. `supervibe:_core:code-reviewer` → review
11. `supervibe:_core:quality-gate-reviewer` → final ≥9 gate

**Bug fix:**
1. `supervibe:_core:root-cause-debugger` via `supervibe:systematic-debugging`
2. `supervibe:tdd` regression test
3. `supervibe:_core:code-reviewer`
4. `supervibe:_core:quality-gate-reviewer`

**Brand reset / new product:**
1. `supervibe:_design:creative-director` → direction
2. `supervibe:_design:prototype-builder` → materialize via `supervibe:brandbook` skill in `.supervibe/artifacts/prototypes/_design-system/`
3. `supervibe:_design:copywriter` + `supervibe:_design:accessibility-reviewer` review
4. User approval → brandbook becomes source-of-truth for all subsequent `supervibe:prototype` invocations

### Mandatory subagent instructions

Every subagent prompt MUST include:

> ANTI-HALLUCINATION: Do not invent files, commands, or contracts that don't exist. Verify every claim with grep/read before reporting.
> CONFIDENCE-DISCIPLINE: Score your output via `supervibe:confidence-scoring` before claiming done. <9 means not done unless `/supervibe-override` was issued with reason.
> ZERO DEAD CODE: Every function/struct/variant/method you create MUST have at least one LIVE call site.

## Verification expectations

- `cd backend && ./vendor/bin/pest` — Pest tests (REQUIRED)
- `cd backend && ./vendor/bin/pint` — code style (REQUIRED, 0 changes)
- `cd backend && ./vendor/bin/phpstan analyse` — static analysis (REQUIRED, level max)
- `cd frontend && npm run typecheck` — TypeScript (REQUIRED)
- `cd frontend && npm test` — Vitest (REQUIRED)
- `cd frontend && npm run lint` — ESLint (REQUIRED)
- `cd frontend && npm run build` — Next.js build (REQUIRED before merge)
- `npm audit && composer audit` — security (REQUIRED before merge)

## Auto-Supervibe triggers

The main agent MUST suggest `/supervibe <phase>` to the user when:

1. Selected host agents folder empty or no routing table → `/supervibe-genesis`
2. New module/command/service created → `/supervibe-adapt`
3. New major dependency → `/supervibe-adapt`
4. Agent failed due to stale context → `/supervibe-audit`
5. >10 files changed in session → `/supervibe-audit`
6. Oldest `last-verified` >30 days → `/supervibe-audit + /supervibe-strengthen`
7. New rule file in the selected host rules folder → `supervibe:_meta:rules-curator` review
8. Override-rate >5% over last 100 entries → `/supervibe-audit`

## Stack-fingerprint

```yaml
project-type: web-app
backend: laravel
frontend: nextjs
database: [postgres]
infra: [redis-cache, queue]
architecture: modular-monolith
design-workflow: html-prototypes
test-pyramid-emphasis: balanced
ci-cd-platform: github-actions
```

(Auto-generated by `supervibe:stack-discovery`. Update via `/supervibe-adapt` if stack changes.)
